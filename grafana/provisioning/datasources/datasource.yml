# Dashboards-as-code: Grafana auto-provisions this datasource on startup,
# no manual "Add data source" click-ops required.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
